apiVersion: v1
kind: Service
metadata:
  name: mywebapp-service
  namespace: mywebapp-ns
spec:
  selector:
    app: mywebapp
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer
